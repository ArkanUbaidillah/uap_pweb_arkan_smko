@extends('layouts.app')
@section('title','Daftar Kursus')
@section('content')
@php($prefix=auth()->user()->role)
<div class="d-flex justify-content-between mb-3"><form class="d-flex gap-2"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari nama/kode kursus"><button class="btn btn-outline-primary">Cari</button></form>@if(in_array(auth()->user()->role,['admin','guru']))<a href="{{ route($prefix.'.courses.create') }}" class="btn btn-primary">Tambah Kursus</a>@endif</div>
<div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Kode</th><th>Nama Kursus</th><th>Guru</th><th>Enroll</th><th>Tugas</th><th>Aksi</th></tr></thead><tbody>@forelse($courses as $course)<tr><td><span class="badge bg-secondary">{{ $course->code }}</span></td><td>{{ $course->name }}</td><td>{{ $course->teacher->name }}</td><td>{{ $course->enrollments_count }}</td><td>{{ $course->assignments_count }}</td><td class="d-flex gap-1"><a class="btn btn-sm btn-info" href="{{ route($prefix.'.courses.show',$course) }}">Detail</a>@if(in_array(auth()->user()->role,['admin','guru']))<a class="btn btn-sm btn-warning" href="{{ route($prefix.'.courses.edit',$course) }}">Edit</a><form method="POST" action="{{ route($prefix.'.courses.destroy',$course) }}" onsubmit="return confirm('Hapus kursus?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Hapus</button></form>@endif @if(auth()->user()->role==='siswa') @if(in_array($course->id,$myEnrollments))<span class="badge bg-success align-self-center">Sudah enroll</span>@else<form method="POST" action="{{ route('siswa.enrollments.store') }}">@csrf<input type="hidden" name="course_id" value="{{ $course->id }}"><button class="btn btn-sm btn-primary">Enroll</button></form>@endif @endif</td></tr>@empty<tr><td colspan="6" class="text-center">Belum ada kursus.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $courses->links() }}</div>
@endsection
